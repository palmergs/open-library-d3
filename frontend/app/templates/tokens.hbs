<div class="container">
  <div class="row">
    <div class="nine columns">
      <div class="row token-comparison">
        {{#if fieldNames}}
          {{charts.n-timeline-chart path=path fieldNames=fieldNames loading=(action 'setLoading')}}
          <label class="centered">Comparison of {{ fieldNames }}</label>
        {{else}}
          <p>Please enter some search terms (comma delimited) to visualize.</p>
        {{/if}}
      </div>
      <div class="row">
        <div class="nine columns">
          <h4>About this project.</h4>
          <p>The data displayed in this app is from the OpenLibrary.com project. The 47 million records available&mdash;authors, works and editions&mdash;were broken out into tokens (ASCII normalized) from the entry titles, subtitles, author names, user descriptions and content excerpts.</p>
          <h4>Future enhancements</h4>
          <ul>
            <li><s>allow for visualization by record type (e.g. only compare author names over time)</s></li>
            <li><s>allow for visualization by category (e.g. only compare user descriptions)</s></li>
            <li><s>allow for different start and end years</s></li>
            <li>UI/UX enhancements: <s>loading indicators</s>, chart legends, transition effects</li>
            <li>allow chart to toggle between linear and logarithmic scale</li>
          </ul>
        </div>
      </div>
    </div>
    <div class="three columns">
      <div class="row">
        <label>Tokens (comma delimited)</label>
        {{one-way-input q 
            focusOut=(action 'setTokens')
            keyEvents=(hash 13=(action "setTokens") 27=(action "resetParam" 'q')) 
            classNames="u-full-width" 
            placeholder="enter terms to search"}}
      </div>
      <div class="row">
        <label>Year Range</label>
        {{one-way-input y
            focusOut=(action 'setStartYear')
            keyEvents=(hash 13=(action "setStartYear") 27=(action "resetParam" 'y'))
            classNames="u-full-width" 
            placeholder="start year"}}
        {{one-way-input e
            focusOut=(action 'setEndYear')
            keyEvents=(hash 13=(action "setEndYear") 27=(action "resetParam" 'e'))
            classNames="u-full-width" 
            placeholder="end year"}}
      </div>
      <div class="row">
        <label>Categories</label>
        <label>
          {{one-way-radio catStr option=null name="category" update=(action "setCategory") }}
          <span class="label-body">All</span>
        </label>
        <label>
          {{one-way-radio catStr option='1' name="category" update=(action "setCategory") }}
          <span class="label-body">Titles and Names</span>
        </label>
        <label>
          {{one-way-radio catStr option='2' name="category" update=(action "setCategory") }}
          <span class="label-body">Content Excerpts</span>
        </label>
        <label>
          {{one-way-radio catStr option='3' name="category" update=(action "setCategory") }}
          <span class="label-body">User Notes and Descriptions</span>
        </label>
      </div>
      <div class="row">
        <label>Data Type</label>
        <label>
          {{one-way-radio t option=null name="type" update=(action "setType") }}
          <span class="label-body">All</span>
        </label>
        <label>
          {{one-way-radio t option='Work' name="type" update=(action "setType") }}
          <span class="label-body">Authors</span>
        </label>
        <label>
          {{one-way-radio t option='Author' name="type" update=(action "setType") }}
          <span class="label-body">Works</span>
        </label>
        <label>
          {{one-way-radio t option='Edition' name="type" update=(action "setType") }}
          <span class="label-body">Editions</span>
        </label>
      </div>
    </div>
  </div>
</div>
