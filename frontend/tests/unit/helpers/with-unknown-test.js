import { withUnknown } from 'frontend/helpers/with-unknown';
import { module, test } from 'qunit';

module('Unit | Helper | with unknown');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = withUnknown([42]);
  assert.ok(result);
});
